# FastAPI and web dependencies
fastapi>=0.110.0
uvicorn>=0.27.0
websockets>=12.0
pydantic>=2.6.0

# ML dependencies - using transformers directly for NumPy 2.x compatibility
transformers>=4.38.0
# Pin NumPy to 1.x for compatibility with PyTorch and other ML libraries
numpy>=1.19.5,<2.0.0
tqdm>=4.67.0
regex>=2023.0.0

# Audio processing
soundfile>=0.13.0
librosa>=0.10.1
sounddevice>=0.4.6

# System monitoring
psutil>=5.9.5

# Speech recognition via transformers (NumPy 2.x compatible)
# Using transformers pipeline instead of whisper
datasets>=2.16.0
accelerate>=0.27.0
tiktoken>=0.9.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.1

# Other utilities
requests>=2.32.0 